<?php
// Đọc giá trị cookie 'fehost'
// Nếu cookie không tồn tại, đặt giá trị mặc định là 'prod-cache-01'
$fehost = $_COOKIE['fehost'] ?? 'prod-cache-01';

// Đặt cookie cho các lần truy cập sau
// (Trong lab thực tế, cookie này có thể đã được đặt bởi một endpoint khác)
if (!isset($_COOKIE['fehost'])) {
    setcookie('fehost', 'prod-cache-01', time() + 3600, '/');
}
?>
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trang chủ Lab</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { padding-top: 4rem; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Cache Lab</a>
        </div>
    </nav>

    <main class="container">
        <div class="bg-light p-5 rounded">
            <h1>Chào mừng đến với Lab Cache Poisoning</h1>
            <p class="lead">Đây là trang chủ. Thông tin host của bạn đang được tải từ một cấu hình động.</p>
            <p>Trang này được phục vụ bởi: Vũ Việt Hoàng</strong></p>
        </div>
    </main>

    <script>
        // LỖ HỔNG NẰM Ở ĐÂY
        // Giá trị cookie được chèn thẳng vào chuỗi JavaScript
        var config = {
            "host": "<?php echo $fehost; ?>"
        };

        // Hiển thị giá trị lên màn hình
        document.getElementById('hostname').innerText = config.host;
    </script>
</body>
</html>